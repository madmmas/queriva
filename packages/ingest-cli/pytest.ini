[pytest]
testpaths = tests
addopts = --cov=. --cov-report=term-missing --cov-fail-under=80
markers =
    unit: no Docker, fast — run via make test-unit
    integration: requires running API — run via make test-int
    slow: large batch ingest tests — run manually
