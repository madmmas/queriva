/** Default date range span for the "Last 7 days" filter chip (issue #20). */
export const DATE_FILTER_RANGE_DAYS = 7;

/** Language code for the Bangla filter chip. */
export const FILTER_LANGUAGE_BANGLA = 'bn';

/** Language code for the English filter chip. */
export const FILTER_LANGUAGE_ENGLISH = 'en';

/** Category value for the National filter chip. */
export const FILTER_CATEGORY_NATIONAL = 'national';

/** Category value for the Weather filter chip. */
export const FILTER_CATEGORY_WEATHER = 'weather';
