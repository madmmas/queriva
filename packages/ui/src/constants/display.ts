/** Number of top-ranked cards that receive highlight border styling (mock UI). */
export const TOP_RESULT_HIGHLIGHT_COUNT = 2;

/** Bangla language badge background colour (mock UI qv-lang-bn). */
export const LANGUAGE_BADGE_BANGLA_BACKGROUND = '#e1f5ee';

/** Bangla language badge text colour (mock UI qv-lang-bn). */
export const LANGUAGE_BADGE_BANGLA_TEXT = '#0f6e56';

/** English language badge background colour (mock UI qv-lang-en). */
export const LANGUAGE_BADGE_ENGLISH_BACKGROUND = '#e6f1fb';

/** English language badge text colour (mock UI qv-lang-en). */
export const LANGUAGE_BADGE_ENGLISH_TEXT = '#185fa5';

/** Minimum score bar width percentage for visibility. */
export const MIN_SCORE_BAR_WIDTH_PERCENT = 0;

/** Maximum score bar width percentage. */
export const MAX_SCORE_BAR_WIDTH_PERCENT = 100;

/** Milliseconds per second for latency display. */
export const MILLISECONDS_PER_SECOND = 1000;

/** Decimal places when formatting relevance scores. */
export const SCORE_DECIMAL_PLACES = 2;

/** Decimal places when formatting seconds in stats panel. */
export const LATENCY_SECONDS_DECIMAL_PLACES = 1;
