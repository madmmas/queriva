# Queriva Ingest CLI

Batch ingestion tool for files, JSON, CSV, and URLs.

Implementation in progress — see issue #9 in `docs/ISSUES.md`.

```bash
python queriva_ingest.py --collection news_radar --format json --source fixtures/news_radar_dhaka_floods.json
```
