{
  "name": "@queriva/api",
  "private": true,
  "scripts": {
    "build": "mvn package -q -DskipTests",
    "test": "mvn test -q",
    "test-unit": "mvn test -q -Punit",
    "test-int": "mvn test -q -Pintegration",
    "clean": "mvn clean -q"
  }
}
