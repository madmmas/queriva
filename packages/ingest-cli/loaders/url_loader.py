"""URL loader — implemented in issue #9."""

from typing import Any


def load(urls: list[str]) -> list[dict[str, Any]]:
    """Fetches and extracts text from URLs with private-network blocking."""
    raise NotImplementedError("URL loader implemented in issue #9")
